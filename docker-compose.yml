# Auto-generated by generate_fleet.py
# OpenManus Containerized Agent Swarm -- 8 services

services:
  prospector:
    build:
      context: ./agents
      dockerfile: Dockerfile
    image: agent-base
    container_name: prospector
    environment:
      - AGENT_ID=prospector
      - AGENT_NAME=PROSPECTOR
      - AGENT_ROLE=Lead research & discovery
      - HEARTBEAT_URL=http://fleet-api:8800/heartbeat
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./agents/prospector/config.toml:/app/config.toml:ro
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net
    restart: unless-stopped

  forge:
    build:
      context: ./agents
      dockerfile: Dockerfile
    image: agent-base
    container_name: forge
    environment:
      - AGENT_ID=forge
      - AGENT_NAME=FORGE
      - AGENT_ROLE=Code generation & building
      - HEARTBEAT_URL=http://fleet-api:8800/heartbeat
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./agents/forge/config.toml:/app/config.toml:ro
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net
    restart: unless-stopped

  lens:
    build:
      context: ./agents
      dockerfile: Dockerfile
    image: agent-base
    container_name: lens
    environment:
      - AGENT_ID=lens
      - AGENT_NAME=LENS
      - AGENT_ROLE=Review, audit & QA
      - HEARTBEAT_URL=http://fleet-api:8800/heartbeat
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./agents/lens/config.toml:/app/config.toml:ro
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net
    restart: unless-stopped

  copywriter:
    build:
      context: ./agents
      dockerfile: Dockerfile
    image: agent-base
    container_name: copywriter
    environment:
      - AGENT_ID=copywriter
      - AGENT_NAME=COPYWRITER
      - AGENT_ROLE=Content & copy generation
      - HEARTBEAT_URL=http://fleet-api:8800/heartbeat
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./agents/copywriter/config.toml:/app/config.toml:ro
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net
    restart: unless-stopped

  herald:
    build:
      context: ./agents
      dockerfile: Dockerfile
    image: agent-base
    container_name: herald
    environment:
      - AGENT_ID=herald
      - AGENT_NAME=HERALD
      - AGENT_ROLE=Outreach & communication
      - HEARTBEAT_URL=http://fleet-api:8800/heartbeat
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./agents/herald/config.toml:/app/config.toml:ro
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net
    restart: unless-stopped

  ledger:
    build:
      context: ./agents
      dockerfile: Dockerfile
    image: agent-base
    container_name: ledger
    environment:
      - AGENT_ID=ledger
      - AGENT_NAME=LEDGER
      - AGENT_ROLE=Logging, tracking & analytics
      - HEARTBEAT_URL=http://fleet-api:8800/heartbeat
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./agents/ledger/config.toml:/app/config.toml:ro
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net
    restart: unless-stopped


  fleet-api:
    build: ./fleet-api
    container_name: fleet-api
    ports:
      - "8800:8800"
    environment:
      - DATABASE_URL=sqlite:///fleet.db
    volumes:
      - fleet_db:/app/data
    networks:
      - swarm-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8800/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  dashboard:
    build: ./dashboard
    container_name: dashboard
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=http://fleet-api:8800
    depends_on:
      fleet-api:
        condition: service_healthy
    networks:
      - swarm-net

volumes:
  fleet_db:

networks:
  swarm-net:
    driver: bridge
